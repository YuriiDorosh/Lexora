# =============================================================================
# Development tooling — install with: pip install -r requirements/dev-requirements.txt
# NOT included in any Docker image
# =============================================================================

# Linting & formatting (ruff replaces flake8 + isort + black + pyupgrade)
ruff==0.4.4

# Type checking
mypy==1.10.0
types-requests>=2.31.0
types-pyyaml>=6.0.12

# Security scanning
bandit[toml]==1.7.8
pip-audit==2.7.3

# Odoo-specific pylint plugin (optional — for backend model audits)
pylint-odoo==9.1.1
pylint==3.2.0

# Pre-commit hooks framework
pre-commit==3.7.1

# Testing
pytest==8.2.0
pytest-cov==5.0.0
pytest-mock==3.14.0
pytest-asyncio==0.23.7
httpx>=0.27.0          # async test client for FastAPI

# Debugging (local dev only)
ipython==8.24.0
ipdb==0.13.13

# YAML / XML linting (local)
yamllint==1.35.1
