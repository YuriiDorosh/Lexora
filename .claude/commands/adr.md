Create a new Architecture Decision Record for: $ARGUMENTS

Steps:
1. Read `docs/DECISIONS.md` and find the highest existing ADR number (e.g. ADR-019 → next is ADR-020).
2. Append the block below to `docs/DECISIONS.md` with the correct number and title.
3. Add a one-line note to the "Decisions made during this milestone" section of `docs/TASKS.md`.

**Block to append:**

---

## ADR-<N> — $ARGUMENTS

**Status:** Accepted

**Context:** <describe the situation or constraint that forced this decision>

**Decision:** <what was decided>

**Reasoning:** <why this option over the alternatives considered>

**Consequences:** <what changes as a result; any trade-offs or follow-up actions>

---

Fill in the four fields based on the current context. Ask me to clarify if the context or reasoning is not clear from the conversation.

After appending, remind me to commit both `docs/DECISIONS.md` and `docs/TASKS.md` together before the next sub-step.
