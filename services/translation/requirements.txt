fastapi==0.115.5
uvicorn[standard]==0.32.1
pika==1.3.2
# M4c (ADR-028): real translation happens through deep_translator, which hits
# a free online provider (default: Google Translate; fallback: MyMemory).  No
# API key required.  Offline Argos is no longer part of the roadmap; see
# ADR-028 for the rationale.  Provider / timeout / fallback are env-driven
# (TRANSLATE_PROVIDER / TRANSLATE_TIMEOUT_SECONDS / TRANSLATE_FALLBACK_PROVIDER).
deep_translator==1.11.4
