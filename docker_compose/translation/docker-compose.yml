services:
  translation-service:
    build:
      context: ../..
      dockerfile: docker_compose/translation/Dockerfile
    container_name: translation_service
    restart: unless-stopped
    ports:
      - "8001:8000"
    env_file:
      - ../../.env
    environment:
      RABBITMQ_HOST: ${RABBITMQ_HOST:-rabbitmq}
      RABBITMQ_PORT: ${RABBITMQ_PORT:-5672}
      RABBITMQ_VHOST: ${RABBITMQ_VHOST:-/}
      RABBITMQ_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_PASSWORD: ${RABBITMQ_PASS:-guest}
    networks:
      - backend
    # RabbitMQ is in a separate compose file; connection_attempts in pika handles retries.

networks:
  backend:
    external: true
